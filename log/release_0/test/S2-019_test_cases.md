# S2-019 测试用例：桌面部署与容器部署配置

**任务名称**: 桌面部署与容器部署配置
**创建日期**: 2026-04-04
**创建者**: sw-mike (Software Tester)
**版本**: 1.0

---

## 测试环境要求

- Docker 20.10+
- Docker Compose 2.0+
- Linux/macOS/Windows with Docker support
- 至少4GB可用内存
- 至少10GB磁盘空间

---

## 测试用例列表

### TC-S2-019-001: Dockerfile.single 语法验证

| 字段 | 内容 |
|------|------|
| **测试类型** | 静态验证 |
| **优先级** | P0 |
| **前置条件** | Dockerfile.single 文件存在 |
| **测试步骤** | 1. 检查 Dockerfile.single 文件存在<br>2. 使用 `docker build --no-cache --dry-run` 或语法检查工具验证Dockerfile语法<br>3. 验证包含必要的构建阶段(backend-builder, frontend-builder, final) |
| **预期结果** | Dockerfile语法正确，包含多阶段构建 |
| **验收标准** | 文件存在且语法验证通过 |

### TC-S2-019-002: docker-compose.yml 语法验证

| 字段 | 内容 |
|------|------|
| **测试类型** | 静态验证 |
| **优先级** | P0 |
| **前置条件** | docker-compose.yml 文件存在 |
| **测试步骤** | 1. 检查 docker-compose.yml 文件存在<br>2. 执行 `docker compose config` 验证配置<br>3. 验证服务定义包含必要的配置项 |
| **预期结果** | docker-compose.yml语法正确，配置完整 |
| **验收标准** | `docker compose config` 执行成功，无错误 |

### TC-S2-019-003: Docker镜像构建测试

| 字段 | 内容 |
|------|------|
| **测试类型** | 构建测试 |
| **优先级** | P0 |
| **前置条件** | Docker环境可用，后端和前端代码已就绪 |
| **测试步骤** | 1. 执行 `docker compose build`<br>2. 等待构建完成<br>3. 检查构建产物 |
| **预期结果** | 镜像构建成功，无错误 |
| **验收标准** | `docker compose build` 返回0退出码 |

### TC-S2-019-004: 容器启动与健康检查

| 字段 | 内容 |
|------|------|
| **测试类型** | 功能测试 |
| **优先级** | P0 |
| **前置条件** | Docker镜像已构建 |
| **测试步骤** | 1. 执行 `docker compose up -d`<br>2. 等待容器启动(最多60秒)<br>3. 执行 `docker compose ps` 检查容器状态<br>4. 执行 `curl http://localhost:8080/health` |
| **预期结果** | 容器运行正常，健康检查通过 |
| **验收标准** | 容器状态为healthy，健康检查返回200 |

### TC-S2-019-005: 端口映射验证

| 字段 | 内容 |
|------|------|
| **测试类型** | 功能测试 |
| **优先级** | P1 |
| **前置条件** | 容器正在运行 |
| **测试步骤** | 1. 执行 `docker compose ps`<br>2. 验证端口8080已映射<br>3. 访问 `http://localhost:8080` |
| **预期结果** | 端口8080正确映射，服务可访问 |
| **验收标准** | 端口映射显示 `0.0.0.0:8080->8080/tcp` |

### TC-S2-019-006: 数据卷持久化验证

| 字段 | 内容 |
|------|------|
| **测试类型** | 功能测试 |
| **优先级** | P1 |
| **前置条件** | 容器正在运行 |
| **测试步骤** | 1. 检查 `./data` 目录是否存在<br>2. 在容器内创建测试文件<br>3. 停止并重启容器<br>4. 验证测试文件仍然存在 |
| **预期结果** | 数据卷正确挂载，数据持久化 |
| **验收标准** | 容器重启后数据仍然存在 |

### TC-S2-019-007: 环境变量验证

| 字段 | 内容 |
|------|------|
| **测试类型** | 配置测试 |
| **优先级** | P1 |
| **前置条件** | 容器正在运行 |
| **测试步骤** | 1. 执行 `docker compose exec kayak env`<br>2. 验证以下环境变量:<br>   - KAYAK_DATA_DIR=/app/data<br>   - DATABASE_URL=sqlite:///app/data/kayak.db<br>   - RUST_LOG=info |
| **预期结果** | 所有必要的环境变量正确设置 |
| **验收标准** | 环境变量值与预期一致 |

### TC-S2-019-008: 部署脚本存在性验证

| 字段 | 内容 |
|------|------|
| **测试类型** | 静态验证 |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试步骤** | 1. 检查 `scripts/start-desktop.sh` 存在<br>2. 检查 `scripts/start-web.sh` 存在<br>3. 检查 `scripts/stop.sh` 存在<br>4. 验证脚本具有可执行权限 |
| **预期结果** | 所有部署脚本存在且具有可执行权限 |
| **验收标准** | 文件存在且 `ls -l` 显示可执行权限 |

### TC-S2-019-009: 部署文档完整性验证

| 字段 | 内容 |
|------|------|
| **测试类型** | 文档测试 |
| **优先级** | P2 |
| **前置条件** | 无 |
| **测试步骤** | 1. 检查 `docs/deployment.md` 存在<br>2. 验证文档包含以下章节:<br>   - 本地部署说明<br>   - Docker部署说明<br>   - 故障排除 |
| **预期结果** | 部署文档完整，包含所有必要章节 |
| **验收标准** | 文档存在且包含必要章节 |

### TC-S2-019-010: 容器停止与清理

| 字段 | 内容 |
|------|------|
| **测试类型** | 功能测试 |
| **优先级** | P1 |
| **前置条件** | 容器正在运行 |
| **测试步骤** | 1. 执行 `docker compose down`<br>2. 验证容器已停止<br>3. 验证容器已移除 |
| **预期结果** | 容器正确停止和清理 |
| **验收标准** | `docker compose ps` 显示无运行中的容器 |

---

## 测试执行计划

### 阶段1: 静态验证 (TC-S2-019-001, 002, 008, 009)
- 验证配置文件和脚本的存在性和语法
- 预计时间: 5分钟

### 阶段2: 构建测试 (TC-S2-019-003)
- 构建Docker镜像
- 预计时间: 10-15分钟

### 阶段3: 功能测试 (TC-S2-019-004, 005, 006, 007, 010)
- 启动容器并验证功能
- 预计时间: 10分钟

---

## 通过标准

- 所有P0测试用例必须通过
- P1测试用例通过率 >= 90%
- P2测试用例通过率 >= 80%

---

## 测试环境清理

测试完成后执行:
```bash
docker compose down -v
docker system prune -f
```

---

**文档结束**
